apiVersion: v1
kind: Service
metadata:
  name: devops-website-service
spec:
  type: LoadBalancer
  selector:
    app: devops-website # This must exactly match the label we gave your pods!
  ports:
    - port: 80
      targetPort: 80